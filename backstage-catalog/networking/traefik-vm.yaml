---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: traefik-proxy
  description: Traefik reverse proxy and Cloudflare tunnel endpoint on pangolin-proxy VM
  annotations:
    grafana/dashboard-url: https://grafana.${INTERNAL_DOMAIN}
  links:
    - url: https://traefik.${INTERNAL_DOMAIN}
      title: Traefik Dashboard
      icon: dashboard
  tags:
    - networking
    - reverse-proxy
    - traefik
    - vm-service
    - overwatch
spec:
  type: vm
  lifecycle: production
  owner: platform-team
  system: sentinel-platform
