apiVersion: v1
kind: Service
metadata:
  name: harbor-database
  namespace: harbor
  labels:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/instance: harbor
spec:
  selector:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/instance: harbor
  ports:
    - port: 5432
      targetPort: 5432
