apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: harbor-postgresql-data
  namespace: harbor
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-storage
  resources:
    requests:
      storage: 5Gi
