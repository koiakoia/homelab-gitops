apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netbox-postgresql-data
  namespace: netbox
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-storage
  resources:
    requests:
      storage: 10Gi
